#include <stdio.h>

int main() {
    int a=1;
    if (printf("%d", &a)) {
        // do nothing
    }
    return 0;
}
 //functions always return something 
 //there is no function that does not return anything

 //a==5
 //so 5=a  constant == variable 
 // variable == variable it should no happen